# Pinned to the versions this project is tested against, so builds are reproducible.
# To upgrade: bump deliberately, run the test suite, then commit the new pin.

# Core
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.4
pydantic-settings==2.14.0
email-validator==2.3.0

# Database
sqlalchemy==2.0.49
alembic==1.18.4
psycopg2-binary==2.9.12

# Auth
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0

# File handling
python-multipart==0.0.27

# Document generation
docxtpl==0.20.2

# Scheduling
apscheduler==3.11.2

# HTTP client (for Telegram)
httpx==0.28.1

# Rate limiting
slowapi==0.1.9
